name: shartflix
description: A movie application with user authentication and infinite scroll feature.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  http: ^0.13.3
  flutter_secure_storage: ^5.0.2
  cached_network_image: ^3.1.0
  flutter_localizations:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter

  intl_utils: ^2.8.4

flutter:
  uses-material-design: true
  generate: true